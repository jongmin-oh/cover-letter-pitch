---
name: cover-letter-pitch
description: >-
  JD(채용공고)를 기준으로 한국어 자기소개서를 정해진 4블록 패턴으로 작성한다.
  `내가 할 수 있는 것 3가지 → 내가 줄 수 있는 것 3가지 → 솔직한 단점 → 마지막 멘트`
  순서로 쓰며, JD와 직접 연관된 내용만 담고 누구나 한 번에 읽히는 문장으로 만든다.
  Use when the user provides a job description and their experience and asks to write
  or revise a 자기소개서 or 자소서.
---

# Cover Letter Pitch

JD를 기준으로 "나를 왜 뽑아야 하는가"를 정해진 패턴으로 정리한 한국어 자기소개서를 쓴다.

## 두 가지 기준

- **가독성**: 비개발자도 한 번에 읽히는 문장으로 쓴다. 한 문장에 한 가지만 담고, 전문용어는 그 기술이 하던 역할로 풀어 쓴다.
- **JD 직접 연관성**: 본문의 모든 내용은 JD의 특정 요구·업무·과제와 직접 연결된다. JD와 짝지을 수 없는 내용은 좋은 경험이라도 넣지 않는다.

이 둘 사이에 순위는 없다. 둘 다 만족하지 못하면 문장을 다시 쓴다.

## 사실관계는 다루지 않는다

자기소개서는 증명 문서가 아니라 제안서다. 사실 확인은 이력서, 포트폴리오, 면접에서 이뤄진다. 따라서 이 스킬은 다음을 하지 않는다.

- 수치의 출처나 측정 기준을 캐묻지 않는다.
- 개인 성과와 팀 성과의 경계를 따지지 않는다.
- 주장-근거 대조표나 사실 검증 단계를 두지 않는다.
- 실패·시행착오 서술을 요구하지 않는다.
- 근거가 약하다는 이유로 문장을 약하게 만들지 않는다.

사용자가 제공한 자료와 답변을 소재로 쓰되, 그 안의 사실 여부를 심문하거나 유보적인 표현으로 방어하지 않는다. **자신 있게 단정해서 쓴다.**

합격을 보장하거나 합격률을 예측하지 않는다.

## Step 0 — 자료 확인

필요한 자료는 **JD 전문**과 **이력서 또는 이에 준하는 경력 설명**이다. 포트폴리오, 지원서 문항, 글자 수 제한, 회사·제품 정보가 있으면 함께 활용한다.

제공된 자료를 먼저 모두 확인하고, 자료에서 알 수 있는 내용은 다시 묻지 않는다. 다음만 결과가 달라질 때 질문한다.

- 지정 문항 또는 분량 제한이 있는지
- 같은 회사·포지션의 기존 파일을 덮어써도 되는지

정보가 부족해도 멈추지 않는다. 자료에서 읽히는 범위로 쓰고, 적용한 가정을 결과와 함께 짧게 알린다.

> 지원자 자료를 별도 프로필이나 데이터베이스에 저장하지 않는다. 파일 저장을 요청받은 경우에만 결과물을 저장한다.

## Step 1 — JD 분석

본문을 쓰기 전에 JD에서 다음을 뽑는다.

| 항목 | 확인 내용 |
|---|---|
| 핵심 업무 | 이 사람이 실제로 하게 될 일 |
| 요구 역량 | 필수·우대로 명시된 능력 |
| 조직의 과제 | 이 포지션을 뽑아서 해결하려는 문제 |
| 일하는 방식 | 협업 방식, 기대하는 주도성 |

여기서 뽑은 항목이 본문 전체의 재료다. 이력서를 먼저 읽고 자랑거리를 고르지 않는다. **JD 항목을 먼저 세우고, 거기에 맞는 경험을 이력서에서 찾는다.**

조직의 과제는 공고에 단서가 있을 때만 쓴다. 회사 내부 문제를 단정하거나 평가하지 않는다.

- 피해야 할 표현: "현재 귀사는 확장성이 부족합니다."
- 가능한 표현: "서비스가 빠르게 커지는 시점에 안정적인 운영을 맡을 사람이 필요하다고 읽었습니다."

## Step 2 — 출력 패턴 (고정)

본문은 항상 아래 네 블록을 이 순서로 쓴다. 블록을 추가하거나 순서를 바꾸지 않는다.

| # | 블록 | 분량 기준 | 핵심 |
|---|---|---|---|
| 1 | 내가 할 수 있는 것 3가지 | 항목당 3~4문장 | JD와 직접 연결된, 내가 잘 할 수 있는 일 |
| 2 | 내가 줄 수 있는 것 3가지 | 항목당 2~3문장 | 나를 뽑으면 조직이 얻게 되는 것 |
| 3 | 솔직한 단점 | 3~4문장 | JD 대비 부족한 점 하나 + 극복 방법 |
| 4 | 마지막 멘트 | 1~2문장 | 조직의 어느 부분에 기여하고 싶은지 |

소제목은 그대로 쓰거나 제출 형식에 맞게 자연스러운 표현으로 바꾼다. 순서와 내용은 고정한다.

네 블록을 합쳐 **A4 한 장**이 되게 쓴다. (→ `형식과 분량`)

### 1. 내가 할 수 있는 것 3가지

JD에서 뽑은 핵심 업무·요구 역량 중 3가지를 고른다. JD에 없는 능력은 아무리 강점이어도 넣지 않는다.

각 항목은 이렇게 쓴다.

1. **할 수 있는 일 선언** (볼드 한 문장): JD의 업무를 내 언어로 옮겨 "이 일을 할 수 있다"고 단정한다.
2. **그렇게 말하는 이유**: 같은 일을 해 본 경험을 짧게 붙인다.
3. **이 회사에서의 모습**: 그 능력이 이 조직의 업무에서 어떻게 쓰일지 한 문장으로 닫는다.

작성 규칙

- **잘 할 수 있다고 쓴다.** "해본 적은 있습니다", "부족하지만" 같은 유보를 붙이지 않는다. 유보는 3번 블록에서 한 번만 한다.
- 세 항목이 서로 다른 JD 요구를 담당한다. 같은 능력을 표현만 바꿔 세 번 쓰지 않는다.
- 기술명 나열이 아니라 할 수 있는 **일**로 쓴다. `Kafka를 다룰 수 있습니다` → `데이터가 몰리는 구간에서 유실 없이 흘러가게 만들 수 있습니다`.
- JD가 명시적으로 요구한 기술명만 그대로 남긴다.

예시(형식 참고용, 소재를 그대로 옮기지 않는다):

> **운영 중인 서비스의 병목을 직접 찾아 없앨 수 있습니다.** 요청을 기다리지 않고 담당자 옆에서 반복 업무를 같이 해보며 어디서 시간이 새는지 확인해 왔습니다. 공고에 적힌 "현장 업무 개선"도 같은 방식으로 접근하겠습니다.

### 2. 내가 줄 수 있는 것 3가지

1번이 "내가 하는 일"이라면 2번은 **"조직이 얻는 것"**이다. 주어가 나에서 조직으로 바뀐다.

각 항목은 이렇게 쓴다.

1. **조직이 얻게 되는 결과** (볼드 한 문장): 사람이 아니라 결과를 쓴다.
2. **왜 지금 이 조직에 필요한지**: JD에서 읽은 상황과 연결한다.

작성 규칙

- 1번 항목을 문장만 바꿔 반복하지 않는다. 능력을 조직의 이득으로 **번역**한다.
- "열심히 하겠습니다", "빠르게 배우겠습니다" 같은 다짐은 줄 수 있는 것이 아니다. 조직에 남는 결과로 쓴다.
- 채용 담당자가 팀에 설명할 수 있는 말로 쓴다. 3개를 이어 읽으면 "이 사람을 뽑으면 이게 생긴다"가 정리돼야 한다.

예시(형식 참고용):

> **기획과 개발 사이에서 통역이 필요 없어집니다.** 요구사항을 문장으로 받아 그대로 만드는 대신, 무엇을 위한 기능인지 먼저 맞추고 시작해 왔습니다. 공고에서 소규모 팀이 빠르게 움직인다고 밝힌 만큼, 중간에서 오가는 확인 비용을 줄이겠습니다.

### 3. 솔직한 단점

JD 요구 중 실제로 부족한 항목 **하나**를 고른다.

1. **인정**: 에두르지 않고 단정한다. (예: "솔직히 대규모 트래픽을 직접 운영해 본 경험은 부족한 것이 사실입니다.")
2. **극복**: 어떻게 메울지 방법으로 쓴다. 인접 경험, 이미 시작한 학습, 비슷한 상황을 통과했던 방식.

작성 규칙

- 단점은 하나만 쓴다. 여러 개를 나열하면 이 블록이 자기 부정으로 읽힌다.
- 지원 자체가 무의미해지는 핵심 요구는 고르지 않는다. 우대사항, 특정 도구, 도메인 경험처럼 메울 수 있는 항목을 고른다.
- **단점을 장점으로 뒤집지 않는다.** "꼼꼼해서 속도가 느립니다" 같은 위장된 자랑은 쓰지 않는다.
- 극복은 의지가 아니라 방법으로 쓴다. 근거 없이 "빠르게 배우겠습니다"로 끝내지 않는다.
- 단점 문장으로 블록을 끝내지 않는다. 마지막은 반드시 극복 문장이다.

### 4. 마지막 멘트

조직의 어느 부분에 기여하고 싶은지 1~2문장으로 선언하고 끝낸다.

- JD에서 읽은 조직의 과제를 지목하고, 거기에 기여하겠다고 쓴다.
- "기회를 주신다면", "최선을 다하겠습니다", "많은 것을 배우고 싶습니다" 같은 수동적 표현은 쓰지 않는다.
- "반드시 성공시키겠습니다"처럼 결과를 보장하지 않는다.
- 앞의 세 블록에서 이미 한 말을 요약하지 않는다. 기여하고 싶은 지점 하나로 닫는다.

예시(형식 참고용):

> 고객사의 실제 업무 흐름에서 문제를 찾고, 현장에서 쓰이는 시스템으로 완성하는 일에 기여하고 싶습니다.

## Step 3 — 문체

- **볼드로 논지를 세운다.** 1·2번 블록의 각 항목 첫 문장을 볼드로 처리해, 볼드만 읽어도 지원 논리가 이어지게 한다.
- **짧고 평이한 문장을 쓴다.** 한 문장에 한 가지만 담는다.
- **전문용어를 풀어 쓴다.** 기술명 대신 그 기술이 하던 역할로 바꾼다. JD가 요구한 기술명만 남긴다.
- **숫자는 주장 뒤에 붙인다.** 숫자로 문장을 시작하지 않는다. 주장을 세우고 근거 자리에 놓는다.
- **단정형으로 쓴다.** "~한 것 같습니다", "~라고 생각합니다" 대신 "~합니다", "~하겠습니다".
- **상투적인 형용사를 쓰지 않는다.** "성실한", "열정적인", "빠른 학습" 대신 그것이 드러나는 행동을 쓴다.

## 형식과 분량

**기본 분량은 A4 한 장이다.** 인쇄하거나 PDF로 변환했을 때 한 페이지에 들어가고, 읽는 사람이 스크롤 없이 한 번에 훑을 수 있는 길이로 쓴다. 지정 문항이나 글자 수 제한이 따로 있으면 그쪽을 우선한다.

- **글자 수**: A4 한 장 = 공백 포함 1,800~2,200자.
- **분량 배분**: 1번 블록이 가장 길고(약 40%), 2번이 그다음(약 35%), 3번과 4번이 나머지(약 25%)를 차지한다.
- **넘칠 때**: 블록을 빼지 않는다. 각 항목의 문장을 줄이고, 근거를 하나로 압축한다.
- **모자랄 때**: 항목을 늘리지 않는다. 각 항목의 근거를 구체적으로 채운다.
- **제한이 A4 한 장보다 짧을 때**: 1·2번 블록의 항목을 3개에서 2개로 줄인다. 블록 자체를 없애지 않는다.
- **문항형 지원서**: 문항이 4블록 중 하나를 직접 묻는다면 해당 블록 내용으로 답한다. 문항 구성이 전혀 다르면 문항을 우선하되, 각 답변 안에서 `JD 연결 → 할 수 있는 일 → 조직이 얻는 것` 순서를 유지한다.

## Step 4 — 자체 평가와 수정 루프

초안을 그대로 내보내지 않는다. 아래 순서를 **기준을 통과할 때까지 반복**한 뒤 최종본만 사용자에게 제공한다.

```
초안 작성
  └─► ① 통과 게이트 (Pass/Fail)
        ├─ Fail → 해당 항목만 고쳐 쓰고 게이트 재실행
        └─ Pass → ② 평가표 채점 (25점 만점)
              ├─ 기준 미달 → ③ 최저 점수 항목부터 수정 → ① 로 복귀
              └─ 기준 충족 → 최종본 확정
```

수정 루프는 **최대 3회**까지 돈다. 중간 버전은 사용자에게 보여주지 않는다.

### ① 통과 게이트

하나라도 `Fail`이면 채점하지 않는다. 해당 부분만 고쳐 쓰고 게이트를 다시 돌린다.

| # | 게이트 | Fail 조건 |
|---|---|---|
| G1 | 블록 구성 | 네 블록이 순서대로 있지 않다 |
| G2 | 항목 수 | 1번 또는 2번이 3가지(분량 제한 시 2가지)를 채우지 못했다 |
| G3 | 단정 | 1·2번 블록에 "해본 적은 있습니다", "부족하지만", "~인 것 같습니다" 같은 유보 표현이 있다 |
| G4 | JD 범위 | JD에 없는 능력·성과가 들어가 있다 |
| G5 | 단점 | 3번의 단점이 둘 이상이거나, 장점으로 뒤집혔거나, 극복 문장으로 끝나지 않는다 |
| G6 | 마무리 | 4번이 기여 선언으로 끝나지 않거나 "기회를 주신다면", "최선을 다하겠습니다"가 들어 있다 |
| G7 | 분량 | A4 한 장(공백 포함 1,800~2,200자) 또는 지정 글자 수 제한을 벗어났다 |

### ② 평가표

다섯 항목을 각 5점으로 채점한다(총 25점). **엄격하게 매긴다. 애매하면 낮은 쪽을 준다.**

| 항목 | 5점 | 3점 | 1점 |
|---|---|---|---|
| **E1. JD 직접 연관성** | 모든 항목이 JD의 특정 문구·업무와 1:1로 짝지어지고, 어느 문장이 어느 요구에 대한 답인지 지목할 수 있다 | 대부분 연결되지만 한두 항목은 "관련은 있다" 수준이다 | 이력서에서 잘한 일을 골라 놓고 JD를 뒤에 붙였다 |
| **E2. 가독성** | 비개발자가 한 번 읽고 각 항목의 뜻을 말할 수 있다. 한 문장에 한 가지만 있다 | 대체로 읽히지만 기술용어나 긴 복문이 두세 개 남아 있다 | 무슨 일을 할 수 있다는 건지 두 번 읽어야 한다 |
| **E3. 1번 블록의 힘** | 세 항목이 서로 다른 요구를 맡고, 각각 "할 수 있다"는 단정과 그 근거가 함께 있다 | 단정은 있으나 근거가 얇거나, 두 항목이 사실상 같은 능력이다 | 기술·업무 나열에 가깝고 무엇을 할 수 있다는 선언이 흐리다 |
| **E4. 2번 블록의 번역** | 세 항목 모두 주어가 조직이고, 1번과 다른 층위의 이득을 말한다 | 한두 항목이 1번을 표현만 바꿔 반복한다 | 조직이 얻는 것이 아니라 다짐·포부가 적혀 있다 |
| **E5. 대체 불가능성** | 회사명과 포지션을 바꾸면 문장이 성립하지 않는다 | 절반 정도는 다른 공고에도 쓸 수 있다 | 어느 회사에 내도 되는 범용 자소서다 |

### ③ 합격 기준과 수정

**완성 조건: 총점 21점 이상 + 모든 항목 4점 이상.** 둘 중 하나라도 못 미치면 수정한다.

수정 규칙

- 가장 낮은 항목부터 고친다. 동점이면 E1 → E5 → E3 → E4 → E2 순으로 처리한다.
- **이미 4점 이상인 항목의 문장은 건드리지 않는다.** 전체를 다시 쓰면 통과했던 항목이 같이 무너진다.
- 같은 항목이 두 번 연속 미달이면 문장을 다듬는 대신 **소재를 바꾼다.** 다른 경험으로 교체하거나, 담당하는 JD 요구를 다른 항목으로 옮긴다.
- 수정 후에는 항상 ① 게이트부터 다시 돌린다. 문장을 고치다 유보 표현이나 분량이 되살아나는 경우가 많다.

3회를 돌고도 기준에 못 미치면 그때 멈춘다. 최종본과 함께 미달 항목·점수·원인을 한두 줄로 알린다. 원인이 근거 부족이라면 표적 질문을 1~3개만 덧붙인다.

- 예: "E1이 3점입니다. 공고의 `데이터 파이프라인 운영` 요구에 맞는 경험이 자료에 없습니다. 관련 경험이 있다면 알려주세요."

## 출력

제출용 본문을 먼저 채팅에 제공한다. 필요하면 본문 밖에 짧게 덧붙인다.

- 글자 수 (공백 포함)
- 자체 평가 총점 (예: `24/25`)
- 적용한 가정

사용자가 파일 저장을 요청한 경우에만 `cover-letters/{회사명}-{포지션}.md`에 저장한다. 다른 경로를 지정하면 그 경로를 따른다. 같은 파일이 이미 있으면 덮어쓰기 전에 확인한다.

저장 파일에는 제출용 본문만 넣는다. JD 분석 같은 내부 자료를 제출 파일의 HTML 주석에 숨기지 않는다. 내부 자료 저장도 요청받으면 `{회사명}-{포지션}-notes.md`로 분리한다.

## 최종 출력 원칙

- 네 블록 패턴을 항상 유지한다.
- 초안을 그대로 내보내지 않는다. 게이트와 평가표를 통과할 때까지 고쳐 쓴다.
- JD와 짝지을 수 없는 내용은 이력서에 남겨두고 자기소개서에서는 뺀다.
- 할 수 있다고 쓸 때는 유보 없이 단정한다. 유보는 3번 블록 한 번뿐이다.
- 단점은 하나만, 솔직하게, 극복 방법과 함께 쓴다.
- 마지막 문장은 조직에 기여하고 싶은 지점 선언으로 끝낸다.
- 사실 검증, 수치 출처 확인, 실패 서술을 요구하지 않는다.
- "합격을 보장한다"거나 근거 없는 우월성을 주장하지 않는다.
- 사용자가 요구한 형식과 분량이 이 스킬의 기본값보다 우선한다.
